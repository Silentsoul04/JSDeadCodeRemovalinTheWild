import React from 'react';
import { withNamespace } from '@ux/namespace-component';

/**
 * Representation of a Header in the Dialog.
 */

export default withNamespace(({ namespace, children, onClick }) => {
  const close = typeof onClick === 'function' && (
    <button tabIndex='0'
      type='button'
      data-action='close'
      className={ namespace('close') }
      data-dismiss='modal'
      aria-label='Close'
      onClick={ onClick }>
      <span aria-hidden='true'>×</span>
    </button>
  );

  return <div className={ namespace('modal-header') }>
    { close }
    <h2 className={ namespace('modal-title') }>{ children }</h2>
  </div>;
});



// WEBPACK FOOTER //
// ./node_modules/@ux/modal/modal-header.js