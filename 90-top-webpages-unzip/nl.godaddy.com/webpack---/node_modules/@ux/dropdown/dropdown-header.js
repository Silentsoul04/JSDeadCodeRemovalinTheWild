import { NamespaceConsumer, NamespaceShape, withNamespace } from '@ux/namespace-component';
import React from 'react';
import PropTypes from 'prop-types';

/**
 * Create a header which can highlight a set of DropdownItems.
 *
 * @class DropdownHeader
 * @displayName DropdownHeader (Web)
 * @public
 */
class DropdownHeader extends NamespaceConsumer {
  render() {
    return (
      <div role='heading' className={ this.namespace('dropdown-header', 'title', 'small') }>
        { this.props.children }
      </div>
    );
  }
}

/**
 * Children should be provided.
 *
 * @enum {Object} propTypes
 * @public
 */
DropdownHeader.propTypes = {
  /** Namespace Consumer props */
  ...NamespaceShape,
  /** Contents of a dropdown header */
  children: PropTypes.node.isRequired
};

export default withNamespace(DropdownHeader);



// WEBPACK FOOTER //
// ./node_modules/@ux/dropdown/dropdown-header.js