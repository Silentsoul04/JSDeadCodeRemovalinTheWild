import { Application } from 't3js';
import solid from './standalone';

/**
 * Make `solid` service available via `BZFD.App.getService('solid')`.
 * This is used in games, @see https://buzzfeed.atlassian.net/browse/SITE-4716
 */
Application.addService('solid', () => solid);

export default solid;
