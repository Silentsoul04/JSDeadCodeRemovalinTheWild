/**
* Prevents functions from firing repeatedly. Will fire each time the threshold is met.
* @param  {Function} fn
* @param  {Number}   threshold
* @param  {Object}   scope
*/
export function throttle(fn, threshold, scope) {
    var last;
    var deferTimer;
    if (!threshold) {
        threshold = 100;
    }
    return function () {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        var context = scope || {};
        var now = +new Date();
        if (last && (now < last + threshold)) {
            clearTimeout(deferTimer);
            var next = threshold + now + 1;
            deferTimer = setTimeout(function () {
                last = now;
                context.deferred = true;
                fn.apply(context, args);
            }, next - last);
        }
        else {
            last = now;
            fn.apply(context, args);
        }
    };
}
//# sourceMappingURL=throttle.js.map


//////////////////
// WEBPACK FOOTER
// ./node_modules/@forbes/fbs-video/dist/utils/throttle.js
// module id = 242
// module chunks = 16