
        if (window && window.performance && typeof window.performance.now === 'function') {
            let currentTime = Math.round(window.performance.now()) !window.MptcfePerf ? window.MptcfePerf = {
                fmp: currentTime
            } : window.MptcfePerf.fmp = currentTime
        }
    