import {
    Application
} from 't3js';
import {
    AdEx
} from '../ex';
import {
    name as stickySidebarAd
} from '../../js/behaviors/sticky-sidebar-unit';

class AdExSticky extends AdEx {
    constructor(context) {
        super(context);
        this.behaviors.push(stickySidebarAd);
    }
}
Application.addModule('ad-ex-sticky', AdExSticky.generateModule());
export {
    AdExSticky
};