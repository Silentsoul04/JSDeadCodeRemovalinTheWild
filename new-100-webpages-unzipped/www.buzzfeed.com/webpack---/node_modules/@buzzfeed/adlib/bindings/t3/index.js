import {
    Application
} from 't3js';
import {
    EventManager,
    publicEvents
} from '../../js/services/events';


publicEvents.trigger = function(event, ...args) {
    EventManager.prototype.trigger.apply(this, arguments);
    Application.broadcast(event, ...args);
};