/**
 * Variables and data shared across client side header bidding set ups Prebid and Amazon TAM
 */

import solid from '@buzzfeed/buzzblocks/js/services/solid/standalone';

export const prebidTimeout = solid.getBreakPoint().match(/xs|sm/) ? 600 : 800;
export const amazonTimeout = 800;