export const chumbox = (proto) => ({
    // override the base method and do nothing since non-GPT units don't define sizes
    setupSizes() {},

    render() {
        this.element.classList.remove('js-ad-collapsed');
        return proto.render.apply(this, arguments);
    }
});