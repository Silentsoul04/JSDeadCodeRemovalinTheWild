export default {
    /**
     * Picks a single item from the array at random.
     * @param {Array} list - array to pick value from.
     * @returns {*} - random item from the provided list.
     */
    choice(list) {
        return list && list[Math.floor(Math.random() * list.length)];
    }
};