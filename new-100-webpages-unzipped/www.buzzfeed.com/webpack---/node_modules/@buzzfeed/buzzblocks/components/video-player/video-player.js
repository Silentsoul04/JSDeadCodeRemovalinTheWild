import {
    Application
} from 't3js';
import VideoPlayerStandalone from './video-player-standalone';

Application.addModule(
    'video-player',
    context => new VideoPlayerStandalone({
        config: context.getConfig(),
        container: context.getElement(),
        broadcast: context.application.broadcast.bind(context.application)
    })
);