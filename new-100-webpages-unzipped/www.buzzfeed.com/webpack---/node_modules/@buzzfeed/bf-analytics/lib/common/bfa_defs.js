"use strict";

/**
 * Object which contains enums with defaults
 * @type {object}
 */
var bfaDefs = {
    /**
     * Enum for platform values.
     * @readonly
     * @enum {number}
     */
    platform: {
        desktop: 1,
        mobileweb: 2,
        bflite: 3,
    },
    /**
     * Enum for BFA mode values.
     * @readonly
     * @enum {number}
     */
    bfaMode: {
        develop: 1,
        prod: 2,
        test: 3,
    },
    /**
     * Enum for tracking behaviors values.
     * @readonly
     * @enum {number}
     */
    trackingBehavior: {
        byDefault: 1,
        ifSpecified: 2,
    },
};
Object.freeze(bfaDefs);
export default bfaDefs;