import {
    createGlobalStyle
} from 'styled-components'
import flashMessage from 'shared/css/flash-message'

const FlashMessageGlobalStyle = createGlobalStyle `${flashMessage}`

export default FlashMessageGlobalStyle