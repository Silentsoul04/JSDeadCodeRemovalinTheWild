import {
    withNamespace
} from '@ux/namespace-component';
import {
    FormElement
} from '@ux/form-element';

/**
 * Interface for creating radio elements. Note: this code is also used by React Native to set the type
 * of the Form Element component.
 *
 * @displayName Radio
 * @class Radio
 * @public
 */
export class Radio extends FormElement {
    constructor(...args) {
        super(...args);

        this.type = 'radio';
    }

    render() {
        return super.render();
    }
}

export default withNamespace(Radio);



// WEBPACK FOOTER //
// ./node_modules/@ux/radio/index.js