import PropTypes from 'prop-types';
import React from 'react';

/**
 * Render the close button of the tooltip.
 *
 * @param {Object} props Component properties.
 * @returns {ReactElement} button
 * @private
 */
export default function TooltipClose({
    close,
    className
}) {
    return <button aria - hidden = 'true'
    type = 'button'
    className = {
        className
    }
    onClick = {
        close
    }
    />;
}

/**
 * Property types definition.
 *
 * @enum {Object} propTypes
 * @private
 */
TooltipClose.propTypes = {
    close: PropTypes.func.isRequired,
    className: PropTypes.string
};



// WEBPACK FOOTER //
// ./node_modules/@ux/tooltip/tooltip-close.js